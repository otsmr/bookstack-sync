
const os = require("os");

// LINUX
if (os.platform() === "linux") {

    // install.sh !! Berechtigung für den Ordner /optbookstack-sync/data
    // rm dist/*.AppImage
    // rm release/linux/*.AppImage
    // npm run buildlinux
    // copy dist/*.AppImage release/linux/BookStack-Sync.AppImage
    // copy docker-compose.yml release/linux/docker-compose.yml
    // copy buld/icons/logo.png release/linux/logo.png

}